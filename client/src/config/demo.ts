/**
 * Demo Mode Configuration
 * 
 * When DEMO_MODE is true:
 * - Wallet functionality is disabled
 * - No RainbowKit/wagmi execution
 * - No SES issues
 * - UI still renders normally
 * 
 * Set to false to re-enable wallets after demo
 */
export const DEMO_MODE = true;

